(()=>{
"use strict";
const q=s=>document.querySelector(s);
const B=()=>window.FlorenceBridge;
let binding=false;
function toast(message){const b=B();if(b?.toast)return b.toast(message);const el=q("#toast");if(!el)return;el.textContent=message;el.classList.add("show");setTimeout(()=>el.classList.remove("show"),2800)}
async function refreshFlorence(){const buttons=[...document.querySelectorAll("[data-refresh-florence]")];buttons.forEach(button=>{button.disabled=true;button.setAttribute("aria-busy","true")});toast("Refreshing Florence…");try{if("serviceWorker" in navigator){const registrations=await navigator.serviceWorker.getRegistrations();await Promise.all(registrations.map(registration=>registration.update().catch(()=>null)))}if("caches" in window){const keys=await caches.keys();await Promise.all(keys.filter(key=>key.startsWith("florence-")).map(key=>caches.delete(key)))}}finally{const url=new URL(location.href);url.searchParams.set("florence_refresh",Date.now().toString());location.replace(url.toString())}}
function ensureRefreshControls(){const topbar=q(".topbar");if(topbar&&!q("#refresh-florence")){topbar.style.gridTemplateColumns="48px minmax(0,1fr) 48px 48px";const button=document.createElement("button");button.id="refresh-florence";button.type="button";button.className="icon-btn";button.dataset.refreshFlorence="true";button.title="Refresh Florence";button.setAttribute("aria-label","Refresh Florence");button.textContent="↻";const bell=q("#bell");if(bell)topbar.insertBefore(button,bell);else topbar.appendChild(button)}document.querySelectorAll("[data-refresh-florence]").forEach(button=>{if(button.dataset.refreshBound==="true")return;button.dataset.refreshBound="true";button.addEventListener("click",()=>void refreshFlorence())})}
async function loadNotifications(){const b=B();if(!b?.db||!b?.profile)return[];const{data,error}=await b.db.from("notifications").select("id,title,body,category,related_record_id,read_at,created_at").eq("recipient_id",b.profile.id).order("created_at",{ascending:false}).limit(100);if(error)throw error;return data||[]}
function openRoster(notification){q('[data-view="roster"]')?.click();setTimeout(()=>{q('[data-roster-tab="mine"]')?.click();if(notification.related_record_id){const target=q(`[data-shift-id="${notification.related_record_id}"]`)||q(`[data-shift="${notification.related_record_id}"]`);target?.scrollIntoView({behavior:"smooth",block:"center"})}},150)}
async function openNotification(notification){const b=B();if(!b?.db)return;try{if(!notification.read_at){const readAt=new Date().toISOString();const{error}=await b.db.from("notifications").update({read_at:readAt}).eq("id",notification.id);if(error)throw error;notification.read_at=readAt}if(String(notification.category||"").toLowerCase()==="roster")openRoster(notification);else toast("Notification marked as read")}catch(error){toast(error?.message||"Florence could not open this notification")}}
async function bindNotificationCards(){if(binding)return;const list=q("#notification-list");if(!list||!B()?.profile)return;binding=true;try{const notifications=await loadNotifications();const cards=[...list.children].filter(node=>node instanceof HTMLElement);cards.forEach((card,index)=>{const notification=notifications[index];if(!notification)return;card.tabIndex=0;card.setAttribute("role","button");card.style.cursor="pointer";if(card.dataset.notificationBound==="true")return;card.dataset.notificationBound="true";card.addEventListener("click",()=>void openNotification(notification))})}catch(_error){}finally{binding=false}}
function start(){ensureRefreshControls();void bindNotificationCards()}
if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",start,{once:true});else start();window.addEventListener("florence:ready",start);window.addEventListener("pageshow",start);window.FlorenceRefresh=refreshFlorence;
})();

(()=>{
"use strict";
// Participant editing is owned by core-ui-fixes-v3. Keeping this legacy block
// inert prevents two observers from adding duplicate controls to the same card.
return;
const q=(selector,root=document)=>root.querySelector(selector);const B=()=>window.FlorenceBridge;const keys=["full_name","preferred_name","date_of_birth","ndis_number","address","phone","emergency_contact","guardian_nominee","gp","pharmacy","communication_needs","diagnoses","allergies","goals","preferences","risks_and_safeguards"];
function toast(message){const b=B();if(b?.toast)return b.toast(message)}
function participantId(){return q("#pf-select")?.value||q("#participant-file-select")?.value||""}
async function openEditor(){const b=B();if(!b?.db||b?.profile?.role!=="supervisor")throw new Error("Only supervisors can edit participant details.");const id=participantId();if(!id)throw new Error("Choose a participant first.");const{data:p,error}=await b.db.from("participants").select("*").eq("id",id).single();if(error||!p)throw error||new Error("Participant record not found.");const f=b.field;const fields=[f("full_name","Full legal name"),f("preferred_name","Preferred name","text",[],false),f("date_of_birth","Date of birth","date",[],false),f("ndis_number","NDIS number","text",[],false),f("address","Residential address","textarea",[],false),f("phone","Participant phone","text",[],false),f("emergency_contact","Emergency contact details","textarea",[],false),f("guardian_nominee","Guardian or nominee","textarea",[],false),f("gp","GP / doctor details","textarea",[],false),f("pharmacy","Pharmacy details","textarea",[],false),f("communication_needs","Communication needs","textarea",[],false),f("diagnoses","Diagnoses","textarea",[],false),f("allergies","Allergies","textarea",[],false),f("goals","Goals","textarea",[],false),f("preferences","Preferences and routines","textarea",[],false),f("risks_and_safeguards","Risks and safeguards","textarea",[],false)];const values=Object.fromEntries(keys.map(key=>[key,p[key]??""]));b.form(`Edit ${p.preferred_name||p.full_name}`,fields,async values=>{const payload={};for(const key of keys)payload[key]=String(values[key]??"").trim()||null;payload.full_name=String(values.full_name||"").trim();payload.date_of_birth=values.date_of_birth||null;payload.updated_at=new Date().toISOString();if(!payload.full_name)throw new Error("Full legal name is required.");const{error:updateError}=await b.db.from("participants").update(payload).eq("id",id);if(updateError)throw updateError;setTimeout(()=>location.reload(),350);return"Participant details updated"},values)}
async function approve(){const b=B();if(!b?.db||b?.profile?.role!=="supervisor")throw new Error("Only supervisors can approve care plans.");const id=participantId();if(!id)throw new Error("Choose a participant first.");if(!confirm("Approve this care plan as the current authorised version?"))return;const now=new Date().toISOString();const{error}=await b.db.from("participants").update({care_plan_approved_at:now,care_plan_approved_by:b.profile.id,updated_at:now}).eq("id",id);if(error)throw error;toast("Care plan approved");setTimeout(()=>location.reload(),350)}
function ensure(){const b=B();if(b?.profile?.role!=="supervisor")return;const hero=q("#pf-content .pf-hero")||q("#participant-file-content .pf-hero")||q("#participant-file-content .participant-file-hero");if(hero&&!q("#edit-participant-details",hero)){const area=hero.lastElementChild||hero;area.style.display="flex";area.style.flexDirection="column";area.style.gap="10px";area.style.alignItems="flex-end";const button=document.createElement("button");button.id="edit-participant-details";button.type="button";button.className="secondary";button.textContent="Edit participant";button.style.background="#fff";button.style.color="#315d46";button.onclick=()=>void openEditor().catch(error=>toast(error.message));area.appendChild(button)}const active=q('[data-pf-tab="care"].active')||q('[data-pf-tab="care-plan"].active');const bar=q("#pf-content .pf-actions")||q("#participant-file-content .pf-actionbar");if(active&&bar&&!q("#approve-care-plan",bar)&&!/Approved/i.test(bar.textContent||"")){const button=document.createElement("button");button.id="approve-care-plan";button.type="button";button.className="primary";button.textContent="Approve care plan";button.onclick=()=>void approve().catch(error=>toast(error.message));bar.appendChild(button)}}
function start(){ensure();const host=q("#pf-content")||q("#participant-file-content");if(host&&!host.__participantActionsObserver){new MutationObserver(ensure).observe(host,{childList:true,subtree:true});host.__participantActionsObserver=true}}
if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",start,{once:true});else start();window.addEventListener("florence:ready",start);window.addEventListener("pageshow",start);document.addEventListener("click",event=>{if(event.target instanceof Element&&event.target.closest('[data-pf-tab],[data-view="participants"]'))setTimeout(ensure,100)});
})();
