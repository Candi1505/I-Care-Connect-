name: Apply Florence invoicing fix

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The Florence invoicing patch has already been applied and validated."
